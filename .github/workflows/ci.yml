---
name: ci

on:
  pull_request:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    # The pinned-hash canary in tests/test_hashing.py is the point of this
    # matrix: it fails if the hash basis moves between interpreters.
    name: test (py${{ matrix.python }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.11", "3.12", "3.13", "3.14"]
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --group dev --python ${{ matrix.python }}
      - run: uv run --python ${{ matrix.python }} pytest -q

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v7
      - run: uv run ruff check .
      - run: uv run ruff format --check .

  keystones:
    name: keystones
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          # C9 compares against the merge base, which needs real history.
          fetch-depth: 0
      - uses: astral-sh/setup-uv@v7
      - run: uv run keystones check --all --format github
      # Skips with a notice unless the token can read branch protection,
      # which the default GITHUB_TOKEN cannot. Set KEYSTONES_ADMIN_TOKEN to
      # a PAT with admin:repo to turn this into a real check.
      - run: uv run keystones doctor --format github
        env:
          GH_TOKEN: ${{ secrets.KEYSTONES_ADMIN_TOKEN || github.token }}
